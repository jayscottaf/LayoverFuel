# LayoverFuel — Claude Code instructions

## Git workflow
- Always commit and push to `main` unless the session instructions or user explicitly specify a different branch.
- Do NOT create feature branches without being asked.
- Railway (server) and Vercel (client) auto-deploy from `main` — pushing to any other branch will not trigger a redeploy.
